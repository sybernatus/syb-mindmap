name: CI

on:
  push:
    branches-ignore:
      - 'main'
      - 'develop'
  pull_request:
    branches-ignore:
      - 'main'
      - 'develop'

jobs:
  test:
    name: "Moon Test & Lint"

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup PATH
        run: |
          echo "${{ github.workspace }}/.bin" >> $GITHUB_PATH
          echo "${{ env.HOME }}/.proto/bin" >> $GITHUB_PATH

      - name: Un-shallow the repository
        run: git fetch --prune --unshallow

      - name: Install dependencies
        uses: actions/cache@v3
        run: bash/repository/setup/index.bash
        with:
          path: |
            .bin
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cargo/bin
            ~/.proto  # ou le dossier où proto installe
          key: ${{ runner.os }}-deps-${{ hashFiles('scripts/install.sh','**/Cargo.lock') }}-proto-${{ env.PROTO_VERSION }}
          restore-keys: |
            ${{ runner.os }}-deps-

      - name: Launch moon test
        run: proto run moon -- ci :test :lint
