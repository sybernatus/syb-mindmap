# yaml-language-server: $schema=./assets/schemas/mindmap.schema.yaml
$schema: ./assets/schemas/mindmap.schema.yaml
data:
  text: GCP
  children:
  - text: Databases
    children:
      - text: Big Query
      - text: Cloud SQL
        style_custom:
          background_color:
            hex: "#FF0000"